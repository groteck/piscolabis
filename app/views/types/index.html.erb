<% content_for :cabecera do%>
    Listing types<%end%>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @types.each do |type| %>
  <tr>
    <td><%= type.name %></td>
    <td><%= link_to 'Show', type, :class => 'esd' %></td>
    <td><%= link_to 'Edit', edit_type_path(type), :class => 'esd' %></td>
    <td><%= link_to 'Destroy', type, :class => 'esd', confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Type', new_type_path, :class => 'esd' %>
