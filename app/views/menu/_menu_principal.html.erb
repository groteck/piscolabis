	<div id="menu">
        <div id="cat"> <%=link_to 'Todo', root_path, :class => 'menu_l'%></div>
            <% if @categories %>
                <% @categories.each do |c| %>
                    <div class="cat"><%= link_to c.name, category_path(c), :class => 'menu_l'%> </div> 
                <%end%>
            <%end%>
        </div>
