<% content_for :cabecera do%>
  Listing Orders
<%end%>

<table>
    <tr>
    <th>id</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
    <tr>
    <td><%= order.id %></td>
    <td><%= link_to 'Show', order, :class => 'esd' %></td>
    <td><%= link_to 'Edit', edit_order_path(order), :class => 'esd' %></td>
    <td><%= link_to 'Destroy', order, :class => 'esd', confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path, :class => 'esd' %>
