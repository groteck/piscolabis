<% content_for :cabecera do%>
  Listing Orders
<%end%>

<table>
    <tr>
    <th>Num Pedido</th>
    <th>Ver Pedido</th>
    <th>Cancelar Pedido</th>
  </tr>
<% if @corders %>
<% @corders.each do |corder| %>
    <tr>
    <td><%= corder.id %></td>
    <td><%= link_to 'Show', corder, :class => 'esd' %></td>
    <td><%= link_to 'Destroy', corder, :class => 'esd', confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
<%end%>
</table>
